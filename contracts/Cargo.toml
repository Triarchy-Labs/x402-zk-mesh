[workspace]
members = [
    "verifier-deposit",
    "verifier-membership",
    "verifier-execution",
    "privacy-pool",
    "guild-registry",
]
resolver = "2"

[workspace.dependencies]
soroban-sdk = "27.0.0-rc.1"
